# Sample canonical technical profile

This sample profile is intentionally generic. Replace it with your own profile before using the app seriously.

The matcher reads:

- `items/profile/technical_experience.json`
- `items/profile/skill_taxonomy.yaml`

Use `scripts/generate_technical_experience.py` as a starter generator when you have repo evidence or notes.
